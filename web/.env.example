# Copie para .env.local e ajuste

# Supabase (obrigatório para /admin com contas de usuário e leitura do catálogo do banco)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Obs: o /admin agora usa contas Supabase (cadastro aberto em /admin/signup).
# A antiga ADMIN_PASSWORD não é mais usada.
